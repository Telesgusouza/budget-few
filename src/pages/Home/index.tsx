import Menu from '../../components/Menu';
import * as Styled from './styled';

export default function Home() {

    return(
        <Styled.Container>
            <Menu />
            
        </Styled.Container>
    );
}
